# Google ADK Course — Python dependencies
# Course runs on the ADK 2.x line (2.0 GA'd 2026-05-19; the classic agent API
# is unchanged, the graph Workflow Runtime is additive). Pin google-adk and
# litellm together; if you bump one, bump the other at the same time.

# ─── Core ADK stack (pinned for classroom reproducibility) ──────────────
# [eval] is needed by M09 (AgentEvaluator). It pulls google-cloud-aiplatform's
# evaluation extra, which caps litellm below 1.86 — that cap sets the litellm
# pin for the whole course.
google-adk[eval]==2.7.1
litellm==1.85.7
openai==2.54.0
python-dotenv==1.0.1
nest-asyncio==1.6.0
deprecated==1.2.18

# ─── Model Context Protocol (M02 tools, M13 A2A demos) ─────────────────
mcp==1.29.0
httpx==0.28.1

# ─── Google GenAI SDK (Part 2: M11-M12 grounding, caching, thinking) ─
# ADK 2.4 requires google-genai >=2.9,<3.
google-genai>=2.9,<3.0

# ─── A2A protocol SDK (M13 — spec 1.0; ADK 2.7.1 accepts >=0.3.4,<2; the [http-server]
# extra is what to_a2a() needs to serve. Verified end-to-end 2026-09-12.) ──
a2a-sdk[http-server]==1.1.2

# ─── Persistence (M08 DatabaseSessionService) ──────────────────────────
# ADK 2.x ships DatabaseSessionService behind the [db] extra; sqlalchemy must
# be installed explicitly. aiosqlite is the async SQLite driver; greenlet is a
# transitive SQLAlchemy async requirement. Required for `sqlite+aiosqlite://` URLs.
sqlalchemy>=2.0,<3.0
aiosqlite>=0.20.0
greenlet>=3.0.0

# ─── Notebook plumbing (local dev; Colab pre-installs Jupyter) ─────────
jupyter>=1.0.0
ipykernel>=6.0.0

# ─── Textbook build (Markdown → HTML) ──────────────────────────────────
markdown>=3.5.0

# ─── Evaluation helpers (M09) ──────────────────────────────────────────
pytest>=8.0.0
